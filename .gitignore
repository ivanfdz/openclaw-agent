# Real secrets: never in the repo
secrets.env

# Gateway state. Treat it as credentials:
# it holds openclaw.json, the SQLite with OAuth tokens in cleartext, and the .env with the gateway token.
state/
workspace/
auth-secrets/

# Upstream clone
repo/
